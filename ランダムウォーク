#complite graph
n, s, k = map(int, input().split())
ad_list = {}
for i in range(1, n + 1):
    a = list(range(1, n + 1))
    a.remove(i)
    ad_list[i] = a

import random

walk = [s]
for _ in range(k):
    s = random.choice(ad_list[s])
    walk.append(s)

print(*walk)
